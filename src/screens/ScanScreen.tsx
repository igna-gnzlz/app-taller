import { ScreenContainer } from "@/src/components/common/ScreenContainer";
import { SectionHeader } from "@/src/components/common/SectionHeader";

export function ScanScreen() {
  return (
    <ScreenContainer>
      <SectionHeader title="Scan screen" />
    </ScreenContainer>
  );
}
